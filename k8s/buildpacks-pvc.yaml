apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: buildpacks-pvc
  namespace: tekton-pipelines
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi  #enough?
