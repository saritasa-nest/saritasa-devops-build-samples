apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  generateName: nodejs-yarn-
  namespace: tekton
  labels:
    app.kubernetes.io/part-of: saritasa-cicd
    saritasa.dev/component: nodejs-yarn
    saritasa.dev/pipeline-role: build-deploy
  annotations:
    pipelinesascode.tekton.dev/on-event:        "[push, pull_request]"
    pipelinesascode.tekton.dev/on-target-branch: "[main, feat/*]"
    pipelinesascode.tekton.dev/on-path-change:  "[nodejs/yarn/**, .tekton/nodejs-yarn.yaml]"
    pipelinesascode.tekton.dev/max-keep-runs:   "5"
    pipelinesascode.tekton.dev/task:         "[https://raw.githubusercontent.com/tektoncd/catalog/main/task/git-clone/0.9/git-clone.yaml, https://raw.githubusercontent.com/tektoncd/catalog/main/task/kubernetes-actions/0.2/kubernetes-actions.yaml]"
spec:
  pipelineRef:
    name: component-delivery
  serviceAccountName: pipeline-sa
  params:
    - name: component-id
      value: nodejs-yarn
    - name: source-subpath
      value: nodejs/yarn
    - name: image-ref
      value: ghcr.io/dknhat2000/nodejs-yarn:{{ revision }}
    - name: commit-sha
      value: "{{ revision }}"
    - name: repo-url
      value: "{{ repo_url }}"
    - name: builder
      value: paketobuildpacks/builder-jammy-base
    - name: build-env
      value: []
    - name: process-type
      value: ""
  workspaces:
    - name: source
      volumeClaimTemplate:
        spec:
          accessModes: [ReadWriteOnce]
          resources:
            requests:
              storage: 2Gi
    - name: git-credentials
      secret:
        secretName: git-credentials
    - name: dockerconfig
      secret:
        secretName: ghcr-credentials
        items:
          - key: .dockerconfigjson
            path: config.json
